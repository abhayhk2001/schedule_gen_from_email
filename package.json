{
  "name": "schedule-gen-from-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "smoke": "node scripts/smoke-pane.mjs",
    "verify": "node scripts/verify.mjs",
    "test:watch": "vitest",
    "icons": "./scripts/build-icons.sh",
    "dev": "vercel dev",
    "deploy": "npm run typecheck && npm test && npm run smoke && vercel --prod",
    "prepare": "node -e \"try { const {execSync} = require('child_process'); execSync('git rev-parse --is-inside-work-tree', {stdio:'ignore'}); execSync('git config core.hooksPath scripts/hooks'); } catch (_) {}\""
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0",
    "vercel": "^59.23.2",
    "vitest": "^2.1.0"
  }
}
