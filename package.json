{
  "name": "schedule-gen-from-email",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "./scripts/build-icons.sh",
    "dev": "vercel dev",
    "deploy": "npm run typecheck && npm test && vercel --prod"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.0.0",
    "typescript": "^5.6.0",
    "vercel": "^59.23.2",
    "vitest": "^2.1.0"
  }
}
